FINAL CHANGE BEFORE CLOSING TASK #49

One final configuration change is required, plus one architectural clarification.

## 1. PRIMARY IMAGE MODEL FOR BETA

Set `gpt-image-1-mini` as the PRIMARY image-generation model for the Beta launch.

Do NOT use `gpt-image-1` as the default primary model for now.

Required:

- OpenAI remains the active provider.
- Primary model = `gpt-image-1-mini`.
- Preserve the existing provider abstraction and `modelId`.
- Make the model selection configurable through the existing provider/config mechanism rather than hardcoding it in multiple places.
- Keep `gpt-image-1` available as an easy future upgrade option.
- Do not change the Hamzawi intent-routing or generation pipeline.

The reason is cost control during Beta.

OpenAI has confirmed that `gpt-image-1-mini` successfully generated a real image with our existing API key during the pre-implementation test.

Use the current successful API shape already validated for `gpt-image-1-mini`.

Also ensure telemetry records:

provider = openai
model = gpt-image-1-mini

and pricing/operational events use the correct model pricing.

Do NOT close the task until the live end-user chatbot generation test is repeated using `gpt-image-1-mini`.

Expected:

END-USER POSTLAB CHATBOT
→ image generation intent
→ OpenAI provider
→ gpt-image-1-mini
→ real image
→ MediaService
→ imageUrl
→ GeneratedImageCard

---

## 2. IMPORTANT: END-USER IMAGE UNDERSTANDING VS IMAGE GENERATION

The final audit identified this existing gap:

`attachedImage` from the paperclip upload is sent to the vision model for analysis, but is NOT currently forwarded to `generateBrandedPost()` as a reference image.

Do NOT implement this feature as part of Task #49 unless it is a minimal safe wiring change.

More importantly, document this distinction clearly:

### Current verified capability

The END-USER POSTLAB CHATBOT can:

- receive an uploaded image
- send it to the vision path
- understand/analyze it

The END-USER POSTLAB CHATBOT can also:

- generate a new image
- use stored Brand Assets as reference images
- use `images.edit()` when stored brand assets are available

### Current known limitation

A newly uploaded paperclip image is not yet automatically passed as a reference image to the image-generation pipeline.

Do NOT mark this limitation as "image understanding broken".

It is specifically:

"Uploaded chat attachment → image generation reference wiring is not yet implemented."

---

## 3. OWNER HAMZAWI MUST REMAIN DISTINCT

Remember that PostLab contains two AI experiences:

A. Hamzawi = private Owner Assistant

B. PostLab Chatbot = End-User Assistant

Task #49's primary acceptance criterion is B.

The provider-layer change may affect both because they share `/api/hamzawi/chat` and `generateBrandedPost`.

Do not redesign or rename either system.

Document this shared-provider impact clearly.

---

## FINAL ACCEPTANCE

Before closing Task #49:

1. Change the default image model to `gpt-image-1-mini`.
2. Run one REAL authenticated generation through the END-USER POSTLAB CHATBOT.
3. Confirm the returned image is generated by `gpt-image-1-mini`.
4. Confirm it is persisted.
5. Confirm it appears in the end-user chat.
6. Confirm telemetry records `gpt-image-1-mini`.
7. Confirm no regression to the owner Hamzawi assistant.
8. Update the validation report with the final model and result.

Do not add unrelated features.
Do not modify the existing intent-routing architecture.
Do not close the task until the real end-user generation test passes with `gpt-image-1-mini`.